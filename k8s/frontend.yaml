apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: micro-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: ahmed3013/frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: micro-prod
spec:
  selector:
    app: frontend
  ports:
    - port: 80
